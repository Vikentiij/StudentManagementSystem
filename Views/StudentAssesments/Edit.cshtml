@model StudentManagementSystem.Models.StudentAssesment

@{
    ViewData["Title"] = "Edit";
    Layout = "~/Views/Shared/DashboardLayout.cshtml";
}

<h1>Edit</h1>

<h4>StudentAssesment</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Id" />
            <div class="form-group">
                <label asp-for="Student" class="control-label"></label>
                <select readonly asp-for="StudentId" class="form-control" asp-items="ViewBag.StudentId"></select>
                <span asp-validation-for="StudentId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Assesment.Title" class="control-label"></label>
                <select readonly asp-for="AssesmentId" class="form-control" asp-items="ViewBag.AssesmentId"></select>
                <span asp-validation-for="AssesmentId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Mark" class="control-label"></label>
                <input asp-for="Mark" class="form-control" />
                <span asp-validation-for="Mark" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
